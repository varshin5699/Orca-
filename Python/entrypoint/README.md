The endpoint is supposed to expose the location where requests can be received and processed. Basically a client-server framework which listens to requests and waits to process and responds by sending the same.

